bin.name = "stdio-fixture"
args = ["more", "--help"]
status.code = 0
stdout = """
Do more, in more detail

Usage: stdio-fixture more [OPTIONS]

Options:
      --force <force>  Do for real
  -h, --help           Print help information (use `-h` for a summary)
"""
stderr = ""
